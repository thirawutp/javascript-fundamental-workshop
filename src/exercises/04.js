class Animal {
  constructor({ name, sound, type }) {
    // ❓ setting name, sound and type as a class attribute
    // 🦊 using this keyword to handle that
  }

  // ❓ implement talk function which return `animal.name says animal.sound`
}

// 💯 convert ES6 class to ordinary ES5 class

export default Animal