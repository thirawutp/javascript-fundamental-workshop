function globalScopeVariable(sentences) {
  if (sentences instanceof Array) {
    // ❓ calculate length sum of all string in array
    // 🦊 using reduce function to sum up all string
    // 🧙‍♂️ https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/reduce

    var totalCharacters = 0
  }

  // ❓ convert totalCharacters to number and combine with string length 
  // 🦊 using + sign to convert any types to number

  return 0
}

export default globalScopeVariable